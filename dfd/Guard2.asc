// interactions with guard 2
function talkGuard2()
{
    cHero.Walk(cGuard2.x + 10,  cGuard2.y + 10, eBlock, eWalkableAreas);
    cHero.FaceCharacter(cGuard2);
    Display("This guard ignores any attempt at conversation.");
}

function lookGuard2()
{
    Display("This guard's face shows irritation.  It's not a pleasant expression.");
}